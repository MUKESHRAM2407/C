#include<stdio.h>
int main()
{
	int i,h,a[10];
	printf("Enter the array elements \t");
	for(i=0;i<10;i++)
	{
		scanf("%d",&a[i]);
	}
	h=a[0];
	for(i=0;i<10;i++)
	{
		if(h<a[i])
		{
			h=a[i];
		}
	}
	printf("The biggest element in an array is %d ",h);
	return 0;
}
