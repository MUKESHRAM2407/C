#include<stdio.h>
int main()
{
	int x,t,i;
	printf("Enter the number ");
	scanf("%d",&x);
	t=1;
	for(i=0;i<x/2;i++)
	{
		if(x%i==0)
		{
			t=0;
		}
	}
	if(t==0)
	{
		printf("%d is not a prime number",x);
	}
	else
	{
		printf("%d is a prime number",x);
	}
	return 0;
}
